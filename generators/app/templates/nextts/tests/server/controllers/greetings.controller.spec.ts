import { sendGreetings } from '../../../server/controllers/greetings.controller';

describe('GreetingsController', () => {
    describe('#sendGreetings', () => {
        it('should response greetings', () => {
            expect(1).toEqual(1);
        });
    });
});
